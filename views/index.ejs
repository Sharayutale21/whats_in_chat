<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Chats</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .chat {
      background-color: aliceblue;
      padding: 20px;
      border-radius: 16px;
      display: inline-block;
      margin: 10px;
      max-width: 300px;
    }
    .msg {
      background-color: dodgerblue;
      color: white;
      padding: 10px 15px;
      border-radius: 16px;
      font-style: italic;
      font-weight: 500;
      margin: 10px 0;
    }
  </style>
</head>
<body class="p-4">

  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2>All Chats</h2>
    <a href="/chats/new" class="btn btn-success">New Chat</a>
  </div>

  <% if (success) { %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      <%= success %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  <% } %>

  <% chats.forEach(chat => { %>
    <div class="chat shadow-sm">
      <p><strong>from:</strong> <em><%= chat.from %></em></p>
      <div class="msg">
        <%= chat.msg %>
      </div>
      <a href="/chats/<%= chat._id %>/edit" class="btn btn-sm btn-warning me-1">Edit</a>
      <form action="/chats/<%= chat._id %>?_method=DELETE" method="POST" style="display:inline;">
        <button class="btn btn-sm btn-danger" onclick="return confirm('Delete this chat?')">Delete</button>
      </form>
      <p class="mt-2">received by: <em><%= chat.to %></em></p>
      <hr>
      <p class="text-muted"><%= chat.created_at.toLocaleTimeString() %></p>
      <p class="text-muted"><%= chat.created_at.toDateString() %></p>
    </div>
  <% }) %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
